<h1> Write a Review! </h1>

<%= form_for([:product, @comment]) do |f| %> <!--note that product is a symbol and not an object-->
  <%= f.label :comment %>
  <%= f.text_area :comment %>
  <br/>
  <%= f.submit %>
<%end%>

